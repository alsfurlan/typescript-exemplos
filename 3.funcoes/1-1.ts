(function() {

    // JavaScript 
    function adicionar(a, b) {
        return a + b;
    }

    let resultado = adicionar(2, 3);
    console.log(resultado);

    resultado = adicionar("a", 3);
    console.log(resultado);
})();