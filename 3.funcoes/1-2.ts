(function() {

    // TypeScript
    function soma(a:number, b:number) {
        return a + b;     
    }

    let resultado = soma(2, 3);
    console.log(resultado);

    // resultado = soma("a", 3);
    // console.log(resultado);

})();